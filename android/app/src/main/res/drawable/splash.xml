<?xml version="1.0" encoding="utf-8"?>
<!-- The pre-Android-12 launch window. A flat fill in the game's own surface
     colour: the system splash (Android 12+) already draws the launcher icon on
     the same colour, so a cold start stays one colour from tap to first frame.
     Replaces the eleven density-bucketed Capacitor-logo PNGs the template
     ships — a ColorDrawable needs no density variants. -->
<color xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/couchLegendBackground" />
