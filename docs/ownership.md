# couch-legend — ownership

> **Status:** `binding`
>
> Generated by substrate-kit. Which area / service / pipeline owns each
> table, event, and write path. **NOT SOURCE OF TRUTH** for code — source
> files always win.

> **Steady state:** this doc's table is **generated** from store / manifest
> specs where those exist — a projection, not hand-prose. This skeleton is
> the interim hand-maintained form until that projection lands.

## Ownership model

Single owner (menno420) owns product, design and every ruling. docs/DESIGN.md is the binding mechanics map — every resource, loop, formula and table is decided there (section 9 = the life-story stage system; section 9.6 = the numeric fairness rails). Sessions decide implementation detail, decide-and-flag reversible design choices, and put genuine product forks to the owner.

## Ownership table

| Area | Owner (module / service) | Writes it owns | Notes |
|---|---|---|---|
| (one row per owned area) | | | |

## New areas

A new area (mechanic, currency, tab, platform shell) is decided in docs/DESIGN.md BEFORE code lands — the repo's founding rule: mechanics mapped and decided first. Anything touching balance or pacing needs simulator evidence (pnpm sim) against the section 9.6 rails before adoption.
